<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.MainActivity"
    android:orientation="vertical"
    >

    <FrameLayout
        android:id="@+id/layout_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_above="@+id/home_main_tab" />

    <LinearLayout
        android:id="@+id/home_main_tab"
        android:layout_width="match_parent"
        android:layout_height="49dp"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal">
        <Button
            android:id="@+id/btn_main_home"
            style="@style/NavibarBtnStyle"
            android:drawableTop="@drawable/home_bg"
            android:background="@color/transparent"
            android:text="首页" />

        <Button
            android:id="@+id/btn_main_live"
            style="@style/NavibarBtnStyle"
            android:background="@color/transparent"
            android:drawableTop="@drawable/live_bg"
            android:text="分类" />

        <Button
            android:id="@+id/btn_main_follow"
            style="@style/NavibarBtnStyle"
            android:background="@color/transparent"
            android:drawableTop="@drawable/follow_bg"
            android:text="关注" />

        <Button
            android:id="@+id/btn_main_mine"
            style="@style/NavibarBtnStyle"
            android:background="@color/transparent"
            android:drawableTop="@drawable/user_bg"
            android:text="我的"
            />

    </LinearLayout>
</LinearLayout>
